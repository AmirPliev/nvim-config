local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "python", "rust", "javascript" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
